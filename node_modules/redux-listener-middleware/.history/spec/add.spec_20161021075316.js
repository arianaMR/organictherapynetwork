import { createStore } from 'redux';

const reducer = (state = 0, action) => {
  switch
}

test('test runner correctly set up', () => {
  expect(true).toBeTruthy();
});

test('redux can be initialized', () => {
  const reducer
  const store = createStore();
  expect(true).toBeTruthy();
});
